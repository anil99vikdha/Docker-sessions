# Docker compose installation
When it comes to Docker Desktop on Windows or Mac it comes by default

# If you are using Linux, docker can be installed by below command but docker-compse cannot be installed from most of the linux distrubutins through yum

# Docker install
yum install docker

# Docker compose 
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
docker-compose --version


# ECR creation and authentication

# Command to configure authenticate with ECR
aws ecr get-login-password --region <your-region> | docker login --username AWS --password-stdin <aws_account_id>.dkr.ecr.<your-region>.amazonaws.com

# command to tag image to ECR registry
docker tag app:3.0 <aws_account_id>.dkr.ecr.<your-region>.amazonaws.com/<repository_name>:tag

# Command to push image to ECR 

docker push 913524921896.dkr.ecr.ap-south-1.amazonaws.com/learn:1.0

# where to verify the token added when login to ECR
~/.docker/config.json


